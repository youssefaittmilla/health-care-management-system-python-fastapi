apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "healthcare-chart.fullname" . }}-test-connection"
  labels:
    {{- include "healthcare-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "healthcare-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
