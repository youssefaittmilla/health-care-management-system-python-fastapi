apiVersion: v1
kind: Service
metadata:
  name: healthcare-service
  namespace: healthcare
  labels:
    app: healthcare-app
spec:
  type: NodePort
  selector:
    app: healthcare-app
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 8000
      nodePort: 30000